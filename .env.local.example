# Sanity CMS Configuration
# Get these values from your Sanity project dashboard at https://sanity.io/manage

# Required: Your Sanity project ID (found in project settings)
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id_here

# Optional: Sanity dataset name (defaults to 'production')
# Override only when using a non-production dataset for testing
NEXT_PUBLIC_SANITY_DATASET=production

# ISR Revalidation Webhook Secret
# Required for on-demand revalidation when content changes in Sanity
# Generate a secure random string with: openssl rand -hex 32
# Must match the secret configured in Sanity dashboard webhook settings
# IMPORTANT: This is a server-side secret - do NOT use NEXT_PUBLIC_ prefix
SANITY_WEBHOOK_SECRET=your_webhook_secret_here
